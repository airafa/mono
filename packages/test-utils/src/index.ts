/**
 * Shared testing utilities for the mono monorepo.
 *
 * Provides Playwright page object conventions, shared test helpers,
 * and common test fixtures.
 */

export { createPageObject, type PageObject } from './page-objects.js';
